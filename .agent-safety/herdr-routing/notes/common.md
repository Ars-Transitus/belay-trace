Operating policy for a routed worker. Resolve the Task and role Note named by the packet. The product native sandbox and approval UI are authoritative; this Note grants no permission.

Implementers work only in the dedicated Git worktree and use Git. Reviewers write returns in the Return workspace and inspect the validated Work checkout read-only. Read the minimum unchanged dependency context needed to understand changed behavior.

Belay is the return channel. Create the role-required linked Work, Review, and Evidence records and return their pointers with one-line status. Do not poll, widen scope, push, deploy, or change external state. Stop for a material unresolved choice or native permission request.
